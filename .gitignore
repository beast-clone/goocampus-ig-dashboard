node_modules
.next
.env
.env.local
.netlify
.DS_Store
*.log
accounts.local.json
CREDENTIALS.md
tsconfig.tsbuildinfo

# Local API-usage counters (Integrations tab)
.data/

# Claude Code — personal (per-user, per-machine) settings + ponytail wiring
.claude/settings.local.json
# graphify generated knowledge graph (rebuild with `graphify .`)
graphify-out/

# gstack engine — the heavy ~1GB program (bun deps + browser binaries) stays
# local per machine; the lightweight skill instruction files under
# .claude/skills/ ARE committed. Reinstall engine elsewhere: cd .claude/skills/gstack && ./setup --local
/.claude/skills/gstack/

# stray env copy (secrets) — never commit
_env.local

# pdfjs worker copied from node_modules by the prebuild step
public/pdf.worker.min.mjs
