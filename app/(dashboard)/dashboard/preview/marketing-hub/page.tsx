// Route entry for the admin Marketing Hub. The implementation lives in ./MarketingHub
// (a plain module) so its view components can be reused by My Workspace — a Next page
// file may only export the default + route config, not the named view components.
export { MarketingHubPage as default } from "./MarketingHub";
