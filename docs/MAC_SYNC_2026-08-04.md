# Mac sync — after the 2026-08-04 history rewrite

On 2026-08-04 the **Windows** session rewrote git history on `feat/dashboard-reskin`
to remove a committed secrets file (`.env.local.bak.*`), then force-pushed
(`bd45931` → `b2960fe`). Any **Mac** clone still has the OLD history and must be
resynced **before** committing/pushing from the Mac — otherwise a stale push would
drag the deleted secret back in.

See `AUDIT_REPORT_2026-08-04.md` for the full context.

---

## Option A — paste into Claude Code on the Mac
```
Context: my Windows session rewrote git history on branch feat/dashboard-reskin of
this repo (goocampus-ig-dashboard) to remove a committed secrets file, then
force-pushed. This Mac clone still has the OLD history. Please sync it, then set up
the two per-machine pieces. Steps:

1. SAFETY FIRST: run `git status`. If there are any uncommitted changes I care
   about, STOP and tell me before doing anything destructive — the next step
   discards local changes.
2. Sync to the rewritten remote:
   git fetch origin
   git checkout feat/dashboard-reskin
   git reset --hard origin/feat/dashboard-reskin
3. Confirm the secret file is gone from history (should print nothing):
   git log --all --oneline -- ".env.local.bak.1785733150"
4. Install the same local pre-commit guard that blocks committing .env* secrets
   (GitHub push protection isn't available on this repo's plan). Write this to
   .git/hooks/pre-commit and chmod +x it:
     #!/bin/sh
     blocked=$(git diff --cached --name-only | grep -E '(^|/)\.env($|\.)' | grep -vE '\.env\.example$')
     if [ -n "$blocked" ]; then
       echo "pre-commit: refusing to commit env/secret file(s):"; echo "$blocked" | sed 's/^/   /'
       echo "Move real values into gitignored .env.local. Bypass: git commit --no-verify"; exit 1
     fi
5. Install Claude-Mem on this machine (it's Windows-only right now):
   npx --yes claude-mem@latest install
6. Read docs/AUDIT_REPORT_2026-08-04.md for the full summary of what changed today.

Then confirm each step done.
```

## Option B — run it yourself on the Mac
The one essential step:
```bash
git fetch origin && git checkout feat/dashboard-reskin && git reset --hard origin/feat/dashboard-reskin
```
Verify the secret is gone from history (prints nothing):
```bash
git log --all --oneline -- ".env.local.bak.1785733150"
```
Nice-to-haves (anytime): install the pre-commit hook from Option A step 4, and
`npx --yes claude-mem@latest install`.

## ⚠️ Caution
`git reset --hard` discards uncommitted changes. If the Mac has unpushed work,
run `git status` there first and stash/commit it before resetting.
